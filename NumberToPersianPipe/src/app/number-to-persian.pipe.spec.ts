import { NumberToPersianPipe } from './number-to-persian.pipe';

describe('NumberToPersianPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToPersianPipe();
    expect(pipe).toBeTruthy();
  });
});
